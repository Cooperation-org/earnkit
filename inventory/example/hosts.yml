---
# Copy the whole inventory/example directory OUT of this repo, fill it in,
# and keep it private. Never commit real values.
all:
  children:
    cohort:
      hosts:
        cohort-services:
          ansible_host: 203.0.113.10        # your fresh Ubuntu 24.04 VM
          ansible_user: root                 # or a sudoer
